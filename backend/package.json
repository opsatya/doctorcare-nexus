{
  "name": "doctorcare-backend",
  "version": "1.0.0",
  "description": "Mock backend for DoctorCare using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 3001 --host 0.0.0.0 --delay 300"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "keywords": ["json-server", "mock-api", "doctorcare", "backend"],
  "author": "DoctorCare Team",
  "license": "MIT"
}